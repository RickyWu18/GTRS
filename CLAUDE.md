# CLAUDE.md

NASA CR-166536（Ferguson, 1988，傾轉旋翼數學模型）之數位化專案。將約 300 頁的
1988 年掃描 PDF 轉為可全文檢索、可交叉追溯的純前端靜態網站。

**權威文件**（有疑義時以此二者為準，勿依本檔推測）
- `docs/CR-166536-數位化專案計畫書-v1.2.md` — 規格與理由
- `docs/CR-166536-WBS-checklist.md` — 工項、完成判準、閘門

本檔只記錄**最容易違反的約束**。引用格式 `計畫書 5.3` 指計畫書該節。

---

## 這是什麼性質的專案

轉錄，不是重寫。三項排除是硬性的：

- **不實作模擬器**——不產出可執行模型
- **不進行數值驗證**——因此無法以「跑出正確配平」反證轉錄正確，品保完全由文件內部提供
- **不重新推導或修正原文**——原文錯漏以 `issues` 標記，不逕行更改

原文本身不完整（缺推導、變數定義不清）。遇到講不通的地方，**標記它，不要補全它**。
補上原文沒有的推導會使資料失去史料價值。

---

## 不可違反的資料層規則

**1　原文與中文嚴禁混雜**

| 欄位 | 只能放 |
|---|---|
| `latex`、`text.en` | Ferguson 原文的忠實轉錄 |
| `text.zh` | 該段原文的對譯（原文有這句話，換成中文） |
| `notes/zh-TW/*.md` | 譯者補充（原文沒有這句話） |

判準：刪去這段會使原文缺一塊 → `text.zh`；刪去只是少了幫助 → `notes/`。
專案跨度數月，混雜後將無法區分何者為原文、何者為當時推測。

**2　`pdf_page` 排序，`printed_page` 顯示**

`printed_page` 一律為**字串**（`"A-12"`、`"87"`），因附錄採 `A-1` 複合編碼無法正確排序。
`original_number` 同理（`"(A-3)"`）。排序、導覽、影像檔名一律用 `pdf_page`（整數）。

**3　符號 key 命名規約**

LaTeX 風格，去反斜線與大括號：`C_T`、`rho`、`Omega`、`theta_75`、`p_dot`、`c_bar`、`V_inf`。

- 大小寫具意義，**不得正規化**（`R` 與 `r` 是不同符號）
- **禁止為消歧而造原文沒有的 key**（不可寫 `T_rotor`）——消歧一律交由 `scope`
- 解析順序：公式所屬 module 的 scope → `scope: global` → 報錯

F1/F2 完全建立在字串完全相等的比對上。命名不一致**不會報錯**，只會靜默少掉一條
交叉引用——這是本專案最難察覺的失效模式。

**4　`condition_symbols` 不得混入 `rhs`**

分段式中只出現於條件的符號（如失速攻角）另存，否則會被誤報為自變數。

**5　一切機器產出一律 `status: draft`**

無論來自 OCR、公式辨識或 VLM。升級判準：

| 狀態 | 判準 |
|---|---|
| `draft` | 機器輸出，未經人眼核對 |
| `reviewed` | 已逐字對照掃描原文核對一次 |
| `verified` | `reviewed` + 參照完整性檢查全數通過 + 無未決 `issues` |

VLM 的幻覺產出語法正確、外觀合理，在無數值驗證的前提下難以事後發現。

**6　schema 裝不下時留標記，不要略過**

```yaml
issues:
  - kind: schema_gap
    note: "此式續至次頁；目前無欄位可記錄其續行範圍"
```

日後補欄位只需檢索 `schema_gap`，不需重讀全本。`status` 不能替代此機制——
狀態保證「抄對」，不保證「抄全」。

`issues.kind` 允許值：`undefined_in_source`、`illegible`、`suspected_typo`、
`cross_page`、`ocr_uncertain`、`schema_gap`。

---

## 凍結：什麼能改，什麼不能

凍結的是**資料 schema 與渲染層對外介面**，不是程式碼行數。

判斷準則只有一條：**該變更是否要求回填既有資料？**

- 為 `js/render/table.js` 增加排序繪圖能力 → 不違反凍結
- 為公式新增一個**必填**欄位 → 違反凍結

| 層級 | 狀態 | 內容 |
|---|---|---|
| `js/render/` | **凍結** | 公式渲染、符號提示、交叉追溯、檢索、掃描對照 |
| `js/app.js`、`js/home.js`、`css/` | 自由變更 | 首頁、導覽、版面、配色 |

三項約束：

1. 設計代幣集中於 `css/tokens.css`
2. 渲染層僅暴露函式（`renderEquation(eq) → HTMLElement`），**不得寫入版面相關的
   CSS class 或容器結構**，由外殼層決定佈局
3. 渲染層遇 schema 未定義的欄位一律**靜默略過**，不報錯、不中止——此規則使日後
   新增選填欄位永不構成破壞性變更

`data/meta.yaml` 的 `schema_version` 僅在真正發生破壞性變更時遞增。

---

## 三道閘門（未通過前下游一律不得開始）

| 閘門 | 內容 | 通過後才能做 |
|---|---|---|
| **G1** | 影像處理參數定案（`scan_spec` 全欄位填妥） | 全本批次轉檔 |
| **G2** | schema 與渲染層介面凍結 | 量產轉錄 |
| **G3** | 全域符號命名空間與 key 規約定案 | 大量寫入 `lhs`／`rhs` |

**G1 尤其不可繞過。** 影像一旦 commit 即永久留存於 git 歷史，重跑只會使其膨脹。
`deskew`／`crop`／`rotate` 三項中途變更會使全本既有 bbox 座標全部作廢
（bbox 採相對座標，不因 DPI 變更失效）。

---

## 工作方式

- **純前端無建置流程**：YAML 於瀏覽器端以 js-yaml 解析。此限制的目的是保障貢獻者
  「commit 即生效」，**不禁止** `generated/` 下由 CI 重建的檔案（檢索索引、檢查結果）。
- **離線工具一律放 `tools/`，統一 Python**。網站執行路徑不依賴它們。
- **每章完成即 commit 上線**，不待全本完成。任何時點停工，已完成部分皆為可用成品。
- **本機無可用 GPU**——Surya／Texify／本地 VLM 不適用，管線選型須據此重新評估
  （計畫書 7.2 的本地方案建議係假設有 GPU）。

## 目前狀態

骨架已建，內容未開始。`data/*.yaml` 為空檔，`data/meta.yaml` 的 `page_map`、
`scan_spec`、`license` 三區塊待實測值回填——**其中的數值不得當作事實引用**。

下一步見 WBS「立即可開始」節：W0.1–W0.4。
