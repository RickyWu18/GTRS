/* 掃描對照（F3）　—— 渲染層（凍結）
 *
 * 依據：計畫書 6.1 F3、5.3.4　│　工項：WBS W2.9
 *
 * 由於本專案排除數值驗證，此功能是唯一的錯誤防線，不得省略。
 *
 * 須支援優雅降級（兩條路徑均須實測）：
 *   有 bbox → 載入整頁影像並以 CSS 裁切至該區域，另提供「檢視整頁」切換
 *   無 bbox → 依 pdf_page 顯示整頁影像
 *
 * bbox 為相對座標 [x1, y1, x2, y2]，原點左上，與影像解析度無關。
 * 格式一律為 list，即使僅有一項（跨頁公式因而無需另立 schema）。
 */

export function renderScan(/* eq */) {
  throw new Error('未實作：WBS W2.9');
}
